import './components/TableComponent.js';

// document.addEventListener('DOMContentLoaded', () => {
    // const table = document.querySelector('foo-table');
    // const data = [
    //     { name: 'Alice', age: 30, location: 'Wonderland' },
    //     { name: 'Bob', age: 25, location: 'Builderland' },
    //     { name: 'Charlie', age: 35, location: 'Chocolate Factory' }
    // ];
    // table.init(data);
    // 
    // console.log('Connect to WS', import.meta.env.VITE_WEBSOCKET_URL);
    // const socket = new WebSocket(import.meta.env.VITE_WEBSOCKET_URL);

    // socket.onmessage = (event) => {
    //     const data = JSON.parse(event.data);
    //     table.addRow(data);
    // };

// });
